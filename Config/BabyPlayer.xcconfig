// Safe committed defaults. A local optional file overrides only this app target.
BABYPLAYER_ASR_API_TOKEN = XX_BABYPLAYER_API_TOKEN
// Xcode 会把未转义的 // 解析为注释；/$()/ 在构建后还原为 //。
BABYPLAYER_ASR_BASE_URL = https:/$()/player.wisteriasoftware.uk/v1
#include? "BabyPlayerSecrets.xcconfig"
