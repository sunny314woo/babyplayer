# BabyPlayer — Current Tasks

> 历史说明：本文件记录项目最早期的 Phase 1 Spike，已不再代表 2026-08-24 的当前任务。
> 当前智能歌词/自动字幕状态和后续顺序见
> [《智能歌词与自动字幕现状审查》](SMART_LYRICS_AUTO_SUBTITLE_AUDIT_2026-08-24.md)；
> Mac 本地分析接力见 [AI_HANDOFF_LYRICS_LOCAL_SERVER.md](AI_HANDOFF_LYRICS_LOCAL_SERVER.md)。

## 2026-08-25 智能歌词当前任务

本节比下方历史 Phase 1 Spike 优先。

- [x] 修复 Mac `8011` 服务退出导致的 Apple TV `-1004`，用 LaunchAgent 登录启动和异常自恢复。
- [x] 修复 D3 缓存未绑定实际 ASR/VAD/候选证据的问题。
- [x] 修复 Web 候选行 ID 不稳定导致 ASR 成功但 DeepSeek 422。
- [x] 实现一次 PCM/WAV 解码、Audio Separator 人声分离、vocals-stem VAD 和整首无人声门控。
- [x] 过滤 vocals stem 中 `BB/DD/Dee/E` 类弱短乐器残留，保留 `He he`、`Bear` 等真实短词。
- [x] 修复 DeepSeek 乱序/重叠/无支持行导致整首 422，并自动回收模型遗漏的有声 ASR 词。
- [x] 真实验收 Who、Baby Shark 和 Wheels；Wheels 开头无人声区零字幕，首句 22.45 秒。
- [x] 将手工 ASR 入口串成 ASR → DeepSeek → 自动启用，增加播放页提示、失败回退与“后发手工选择优先”保护。
- [x] 实现功能键排上键立即收起、字幕默认开启、DeepSeek 已有结果默认优先，以及采用成功卡 5 秒自动隐藏。
- [x] Python 54 项和 tvOS 模拟器 58 项全部通过。
- [ ] 建立 10–20 首人工标注回归集：每句文字/首尾、人声区间、段落类型和重复次数。
- [ ] 用标注集 A/B 腾讯人声轨与 faster-whisper/WhisperX，评估第二 ASR/可信歌词强制对齐。
- [ ] 统一 ASR SRT、Apple TV ASR 和 DeepSeek 的 canonical cue/分行器。
- [ ] 将尾段人声、重复副歌数量、时间误差和错词率变为自动验收指标，英文稳定后再生成双语字幕。

## 当前阶段

**Phase 1：最小技术 Spike**

V1 需求与 UI 已冻结，允许进入开发。当前先验证 Jellyfin 与 tvOS 系统播放器链路，不提前实现完整 UI。

## 已确认

- [x] 明确产品基本定位
- [x] 明确儿童优先原则
- [x] 明确参考 Infuse 的内容优先 UI 思路
- [x] 明确 VLC / 通用文件播放器不是目标体验
- [x] 明确控制 V1 范围
- [x] 排除 V1 AI、云同步、社交等非核心能力
- [x] 确认 V1 媒体来源 → 局域网 Mac Mini 上的本地文件
- [x] 确认 Apple TV 与本地媒体之间的数据链路 → V1 使用 Jellyfin HTTP API + Quick Connect 配对；V2 可扩展
- [x] 确认是否需要 Mac 配套端 → V1 不需要，使用 Jellyfin
- [x] 确认 V1 视频 / 音频支持范围 → 只支持视频，不支持纯音频（MP3/AAC 等）；视频格式由 Jellyfin 决定
- [x] 确认首页信息架构 → 顶部固定三个分类选项卡 + 中间两行 shelf（白天模式行 / 哄睡模式行），两行对应两种播放模式入口；选项卡切换后两行独立过滤
- [x] 确认封面比例 → 横版 16:9
- [x] 确认播放器方案 → tvOS 系统 AVPlayerViewController
- [x] 确认最小播放控制能力 → 开始播放、暂停/继续、退出、返回；V1 不记录播放进度
- [x] 确认家长设置入口 → 首页角落低调入口；V1 不加 PIN 保护
- [x] 确认分类选项卡 → 固定三个：儿歌 / 动画 / 全部；切换后两行独立过滤
- [x] 确认播放模式视觉差异 → 白天使用浅杏黄和珊瑚橙，哄睡使用淡紫粉和莓果紫
- [x] 确认播放结束提示 → 不显示剩余时间；循环次数或停止条件达到后直接结束
- [x] 确认设置页结构 → 使用平铺列表，不分组
- [x] 确认空状态 → 大图标面向儿童，小字说明面向家长
- [x] 确认白天模式默认参数 → 每首视频默认循环 3 次，家长可调整
- [x] 确认哄睡模式默认参数 → 默认总时长 30 分钟，曲目不循环，家长可调整
- [x] 确认封面缺失策略 → Apple TV 端从视频画面截取生成
- [x] 确认行标题 → “白天模式”“哄睡模式”保持对儿童可见
- [x] 确认家长入口 Focus 路径 → 从第二行最后一个封面按“右”到达齿轮
- [x] 确认首页 Back 行为 → 首页拦截 Back，防止儿童误退出
- [x] 确认内容适用性标签 → `daytime` / `sleep` / `both`；默认 `both`，可同时出现在两行；Jellyfin 文件夹提供初始分配，设置页提供手动兜底
- [x] 确认最终视觉与交互基准 → 本地 `BabyPlayer_Design_Prototype/` HTML 原型，视觉方向 C「绘本果园」；旧 Figma 文件不作为事实来源
- [x] 确认哄睡自然结束规则 → 当前曲目结束后自动播放下一首，每首最多一次，直到总时长或列表结束
- [x] 确认 V1 不做点赞 / 禁用标记 → 移入 Backlog
- [x] 确认首次配置 Back → 逐步返回，不能退出或绕过配置
- [x] 确认轻柔遥控器声音反馈 → V1 保留，家长设置可关闭
- [x] 完成 V1 需求冻结并允许进入开发

## 当前技术 Spike

- [x] 安装并启动 Jellyfin 10.11.11（Apple Silicon）
- [x] 完成 Jellyfin 初始化并扫描至少一条测试视频（当前已扫描 3 条 MP4）
- [ ] 创建最小 tvOS SwiftUI 工程并通过 `xcodebuild`
- [ ] 验证 Jellyfin Quick Connect
- [ ] 获取一条真实测试视频
- [ ] 使用系统 `AVPlayerViewController` 播放
- [ ] 验证播放页与首页的 Apple TV 遥控器 Back 行为

## 当前唯一任务

**跑通 Jellyfin Quick Connect → 获取一条真实视频 → 系统播放器播放 → Back 返回的端到端链路。**

Spike 通过后依次实现：数据模型和 Jellyfin 访问、首次配置、儿童首页、Focus 与 shelf 滚动、白天/哄睡播放规则、家长设置、空状态和错误状态。每个阶段都必须可构建、可运行、可验证。

## Backlog

以下内容只记录，不代表 V1 承诺开发：

- AI 分类
- 智能推荐
- 生活场景
- 故事生成
- 更复杂的媒体来源（自建配套端、NAS、USB 硬盘直连等）
- 云同步
- 多用户
- 纯音频支持（MP3/AAC）+ 双语字幕，用于桥梁书、章节书、原版小说阅读（竞品参考：微信读书）
- 智能排序（基于点赞率、退出行为、记忆曲线、按播放最少推荐）
- PIN 家长锁
- 播放进度记录 / 继续播放
- 儿童自建播放列表
- 点赞 / 禁用标记及其排序影响

Backlog 中的项目不得顺手实现。
